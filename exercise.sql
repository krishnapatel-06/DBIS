-- create database exercise;
use exercise;

-- create table student(
-- student_id int primary key,
-- stu_name varchar(50),
-- age int,
-- course text,
-- marks int
-- );

-- insert into student(student_id,stu_name,age,course,marks) values
-- (1,'riya',20,'java',85),
-- (2,'amit',22,'python',90),
-- (3,'arjun',19,'c++',78),
-- (4,'priya',21,'sql',88);

-- select * from student;

-- select stu_name,age from student;

-- select * from student where marks > 80;

-- select * from student order by marks desc;

-- update student set marks = 95 where stu_name = 'riya';

-- select sum(marks) as total_marks from student;

-- select course,avg(marks) as avg_marks from student group by course;

-- update student set course = 'java' where student_id = 3;

-- delete from student where student_id = 4;